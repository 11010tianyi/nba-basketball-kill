# NBA 篮球杀卡牌视觉设计语言及规范

> 目标：把手牌从“纯色 + 文字”升级为有篮球语义的卡牌系统。卡面要能在 116×186 的小尺寸里快速识别功能，同时保留三国杀式牌名、花色点数和效果文字的可读性。

## 设计关键词

- **球场战术感**：半场线、三分弧、篮筐、球路、X/O 战术点。
- **体育卡收藏感**：局部高光、队色能量、金属装备光泽。
- **小尺寸优先**：卡牌在手牌区很小，背景图不能抢走牌名和描述。
- **功能一眼可辨**：不同类型牌使用不同图形母题，而不仅靠颜色。
- **两套音效一致**：街机合成与球场现场都要覆盖同一批游戏事件。

## 卡面信息层级

1. 顶部：类型、花色、点数。字号小但必须清楚。
2. 中部：牌名。最强视觉锚点。
3. 中央图形：圆形图标保留，但背景图承担“功能氛围”。
4. 底部：效果说明。最多两行半，不能被背景压暗。
5. 状态层：不可用、拖拽、弃牌选中、目标提示必须压过背景。

## 功能图形母题

| 类型 | 视觉母题 | 用途 |
|:---|:---|:---|
| 进攻 | 篮球轨迹、三分弧、突破斜线 | 突破、三分、快攻 |
| 反应 | 交叉封堵线、冷色护盾、盖帽弧线 | 防守、盖帽、教练挑战 |
| 战术 | 战术板网格、X/O 点位、传球路线 | 助攻、抢断、包夹、犯规、鼓舞 |
| 治疗 | 医疗十字、暂停灯光、罚球轨迹 | 暂停、罚球 |
| 装备 | 金属高光、鞋底弧、戒指圆环、护具纹理 | 球鞋、护臂、战术板、冠军戒指 |

## 色彩规范

- 保留卡牌原本功能色作为 `--card-color`，背景图使用白色、金色和低透明度高光叠加。
- 禁止让整张卡只剩一种色相；每张卡至少有一层白色/金色/冷色中性光。
- 牌名区域优先保证对比度，背景图不穿过牌名主体。
- 金色只用于特殊价值：冠军戒指、专属聊天、关键奖励。

## GPT-image-2 生成提示词规范

如后续要用 GPT-image-2 批量生成位图背景，统一使用 2:3 竖版、无文字、无人物、无商标、无真实球队 logo 的抽象篮球图形。

通用提示词：

```text
Create a premium abstract basketball trading card background, vertical 2:3 ratio, no text, no logos, no players, readable center area, dark sports UI compatible, layered court lines, subtle light bloom, clean vector-like illustration, high contrast but not busy.
```

类型补充：

- 进攻：`orange basketball motion trail, rim arc, explosive diagonal lane`
- 反应：`cool blue defensive cross lines, block silhouette geometry, shield rhythm`
- 战术：`coach board X and O routes, dotted passing lanes, tactical grid`
- 治疗：`warm timeout glow, medical cross geometry, free throw spotlight`
- 装备：`metallic gear shine, sneaker sole curve, championship ring halo`

负面约束：

```text
No readable text, no NBA/team logos, no real player likeness, no clutter, no photorealistic crowd, no tiny details that disappear at card size.
```

## 当前实现

当前版本先用 CSS 分层渐变实现“可生成图像风格”的卡面背景，优点是：

- 无外部版权风险；
- 小尺寸下清晰；
- 可随主题色自动变化；
- GitHub Pages 静态部署无需额外资源。

后续若生成 PNG/WebP/JPG 背景图，建议按“背景包”放入 `assets/cards/<包名>/`。包名可以是 `cartoon`、`china`、`dark` 等任意文件夹名；本地服务器会自动扫描所有子文件夹，并在游戏设置里的“卡牌背景”中显示。

```text
assets/cards/cartoon/offense.webp
assets/cards/cartoon/reactive.webp
assets/cards/cartoon/tactic.webp
assets/cards/cartoon/heal.webp
assets/cards/cartoon/equip.webp
assets/cards/cartoon/utility.webp
```

支持的文件名为：

- `offense`：进攻牌
- `reactive`：反应牌
- `tactic`：战术牌
- `heal`：治疗牌
- `equip`：装备牌
- `utility`：其他牌

支持的格式为 `.png`、`.webp`、`.jpg`、`.jpeg`。某个包缺少某类背景时，会继续保留 CSS 渐变作为加载失败或缺图兜底。

## 音效事件规范

两套音效都必须覆盖以下事件：

- `bounce`：突破、运球、鞋启动
- `shoot`：三分出手
- `swish`：命中、治疗成功
- `block`：盖帽、防守成功
- `whistle`：暂停、犯规、挑战
- `equip-shoes`：穿鞋
- `equip-sleeve`：护臂
- `equip-board`：战术板
- `equip-ring` / `ring`：装备或触发冠军戒指
- `death`：离场
- `chat`：普通聊天
- `signature`：球星专属金色发言

街机合成音效使用 Web Audio 分层音色；球场现场音效用现有真实片段组合，保证本地资源轻量。

## 聊天文案规范

- 普通快捷语：偏虎扑/NBA 球迷文化，但避免辱骂、人身攻击和过度黑称。
- 表情包：短、强反馈、适合移动端点击。
- 球星专属语：金色、高亮、符合球星气质，优先名场面、打法和球迷熟悉梗。
- 每条不超过 20 个中文字或 90 个字符，避免挡住战术日志。

## 可维护规则

1. 新增卡牌时必须指定类型母题，不允许只给纯色。
2. 新增卡牌背景包时，至少提供 1 个受支持文件名；推荐一次补齐 6 类。
3. 新增音效事件时，街机和现场两套都要有 fallback。
4. 新增球星时至少补 3 条专属聊天语。
5. 所有聊天语要保持球迷氛围，不做攻击性黑话默认快捷项。
